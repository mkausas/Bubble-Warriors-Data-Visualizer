army:
- name
- color
- size
- troop type
- save these settings
- load a previously saved army

required:
- team color
- team count
- army types

red 
normal (219, 22, 55)
hover (212, 81, 114)

blue 
normal (53, 60, 211)
hover (46, 119, 255)

green
normal (31, 217, 74)
hover (24, 255, 133)

yellow
normal (241, 243, 39)
hover (234, 255, 98)

git add -f *
git commit -m "blah blah"
git push -u origin master



git init
git remote add origin https://github.com/try-git/try_git.git
git pull origin master
git branch clean_up // make a new branch
git checkout clean_up // switch to clean_up branch
git checkout master // switch to master branch
